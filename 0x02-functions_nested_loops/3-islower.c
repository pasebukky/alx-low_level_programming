#include "main.h"

/**
 * _islower - Check for lowercase characters
 * 
 * Return: 1 if the character is lowercase and 0 otherwise
 */
int _islower(int c) /*@c: The character to be checked/
{
    if (c >= 'a' && c <= 'z')
    {
        return 1;
    }
    else
    {
        return 0;
    }
}
