/**
 * _abs - Computes the absolute value of an integer
 * @r: Integer used to compute the absolute value
 *
 * Return: Absolute value of r
 */
int _abs(int r)
{
	if (r < 0)
		return (-r);
	else
		return (r);
}

