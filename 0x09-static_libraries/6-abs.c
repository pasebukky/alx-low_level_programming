#include "main.h"

/**
 * _abs - Prints an integers absolute value
 * @n: Integer whose value is being printed
 *
 * Return: Absolute value of n
 */

int _abs(int n)
{
    if (n < 0)
    {
        return -n;
    }
    else
    {
        return n;
    }
}

